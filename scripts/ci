#!/usr/bin/env bash
set -euo pipefail

# Lightweight default CI entrypoint for this public project.
# Keep this green until project-specific tests are added.
echo "No project-specific CI tests configured yet. Passing by default."
exit 0
