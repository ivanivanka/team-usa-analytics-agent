# =============================================================================
# TODO 1: Configure Your Environment
# =============================================================================
# Fill in your lab-specific values below. These variables are used by both the
# MCP Toolbox (to connect to AlloyDB and BigQuery) and the ADK agent (to
# connect to Vertex AI).
#
# After editing, activate with: source .env

# --- Your Lab Values (fill these in) ---
export PROJECT_ID="__PROJECT_ID__"
export REGION="__REGION__"
export DB_USER="__USER_EMAIL__"

# --- AlloyDB Connection (match your Terraform deployment) ---
export ALLOYDB_CLUSTER="team-usa-cluster"
export ALLOYDB_INSTANCE="team-usa-primary"
export ALLOYDB_DATABASE="team_usa"

# --- ADK / Vertex AI ---
export GOOGLE_CLOUD_PROJECT="${PROJECT_ID}"
export GOOGLE_CLOUD_LOCATION="${REGION}"
export GOOGLE_GENAI_USE_VERTEXAI="TRUE"
